# Database Package

PostgreSQL migrations and schema notes for AI Portfolio Advisor.

Tenant isolation is enforced by schema design and repository filters. PostgreSQL row-level security can be enabled once the production auth claims and connection strategy are finalized.
